import silly_cate from "./imgs/silly_cate.jpg";

const Imagem = () => {
    return (
        <div>
            <img src={silly_cate} alt="Silly Cate" />
        </div>
    );
};

export default Imagem;
